/* Site configuration.
 * If you deploy to a different repo, change these — the admin panel also lets
 * you override them at runtime (saved in your browser only). */
window.SITE_CONFIG = {
  owner: "advocateshaliniofficial",
  repo: "advocateshaliniofficial.github.io",
  branch: "main"
};
